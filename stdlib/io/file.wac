
record File {
    # file descriptor
    fd i32
}

var STDIN = new File(0)
var STDOUT = new File(1)
var STDERR = new File(2)

fn File.Write(self, data id) {
    var len = SizeOfRawData(data)
    var ptr = PointerToRawData(data)
}
