# malloc/free stuff
# This file needs to come first because it needs to initialize all
# its global variables before any memory can be allocated on the heap


fn __malloc(len i32) i32 {
    return 0
}

fn __free(len i32, ptr i32) {
}
